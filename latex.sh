#!/usr/bin/env bash
# Getting latex to work

# Install pdflatex
sudo apt-get install texlive-latex-base

# Get packages.
# If you need to install something more, just type the name of the package in the package manager, and install it. 
sudo apt-get install texlive
sudo apt-get install texlive-latex-extra
sudo apt-get install latex-xcolor
sudo apt-get install pgf # To get tikz
sudo apt-get install texlive-lang-norwegian

